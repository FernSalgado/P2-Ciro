java -cp hsqldb.jar org.hsqldb.server.Server --dbname.1 petbook --database.1 file:petbook // Roda o servidor

java -cp hsqldb.jar org.hsqldb.util.DatabaseManager // Abre o manager do banco

create table Pet (id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, nome VARCHAR(100), sexo CHAR,porte CHAR, tipo CHAR, castrado CHAR)  //Cria tabela

